repository: takumin/boilerplate-golang-cli
body:
  if: false
summary:
  if: true
comment:
  if: is_pull_request
push:
  if: is_pull_request
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
report:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
coverage:
  badge:
    path: docs/coverage.svg
codeToTestRatio:
  badge:
    path: docs/ratio.svg
  code:
  - '**/*.go'
  - '!**/*_test.go'
  test:
  - '**/*_test.go'
testExecutionTime:
  badge:
    path: docs/time.svg
