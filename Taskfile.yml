---
# https://taskfile.dev

version: '3'

includes:
  go: ./tasks/go.task.yml

tasks:
  default:
    silent: true
    deps:
    - task: go
  test:
    silent: true
    deps:
    - task: go:{{.TASK}}
  build:
    silent: true
    deps:
    - task: go:{{.TASK}}
  clean:
    silent: true
    deps:
    - task: go:{{.TASK}}
